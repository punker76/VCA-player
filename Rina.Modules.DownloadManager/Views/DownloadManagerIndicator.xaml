<UserControl x:Class="Rina.Modules.DownloadManager.Views.DownloadManagerIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border Background="{DynamicResource AccentColorBrush}"
                CornerRadius="3">
            <TextBlock Foreground="{DynamicResource AccentSelectedColorBrush}"
                       Margin="5 2"
                       Text="{Binding DownloadService.DownloadCount}" />
        </Border>
    </Grid>
</UserControl>
